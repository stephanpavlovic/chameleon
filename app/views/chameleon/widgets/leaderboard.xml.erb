<?xml version="1.0" encoding="UTF-8"?>
<root>
  <% @data[:items].each do |item| %>
    <items>
      <value><%= item[:value] rescue "" %></value>
      <label><%= item[:label] rescue "" %></label>
    </items>
  <% end %>
</root>
